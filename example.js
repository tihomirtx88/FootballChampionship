const separator = (players) => {
    players.reduce((acc, player) => {
      if (player === "GK") {
        const newValye = acc[0].push(player)
      }
    }, [[], [], [], []])
  }

  const mappingPositions = (separatedPlayers) => {
    separatedPlayers.map((p) => {
      if (p.length === 1) do somthing
      if (p.length === 2) do somthing
    })
  }
